<% provide(:title, 'Home') %>

<%= link_to image_tag("sc.png", alt: "Rails"), 'http://searleconsulting.com/' %>

<% if signed_in? %>
    <br>
    <br>
    <div>
      <%= form_tag people_path, :id => "filter_tags", :method => 'get', :remote => true do %>
          <% Tag.all.each do |a| %>
              <%= check_box_tag("tags[]", a.id, false, :class => 'checkboxes') %>
              <%= h a.name %>
          <% end %>
      <% end %>
    </div>
    <div id="people" class="row">
      <%= render 'people/people' %>
    </div>
    <div>    <%= link_to t('.new', :default => t("helpers.links.new")),
                         new_person_path,
                         :class => 'btn btn-primary' %></div>
<% else %>
    <div class="center hero-unit">
      <h1>Welcome</h1>

      <h2>
        This is the CRM framework for the
        <a href="http://searleconsulting/">Searle Consulting</a>
        web app.
      </h2>

      <%= link_to "Sign up now!", signup_path,
                  class: "btn btn-large btn-primary" %>
    </div>
<% end %> 